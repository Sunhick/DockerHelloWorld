# Design

This branch contains a design files created using UMLet. Also this branch is marked as an orphan branch so you cannot accidentally merge from other existing branches such as master, dev etc.

```sh
$ git checkout --orphan design
```
